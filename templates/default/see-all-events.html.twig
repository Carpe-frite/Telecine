{% extends "base.html.twig"  %}

{% block main %}
    <main class="flex flex-grow relative w-[1000px] h-min-view left-1/2 -translate-x-1/2 mt-6 sm:mt-12 pb-24">

    <div id="column-2" class="justify-between">
        <div id="latest-events" class="grid grid-cols-2 gap-y-4 gap-x-4">
            {% for event in events %}
                {% include 'partials/_event_container.html.twig'  %}
            {% endfor %}
        </div>
    </div>
        


    </main>

{% endblock %}
